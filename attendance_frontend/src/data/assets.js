// Served by Frappe at runtime from bb_tution_management/public/images, so this
// must stay a plain string used via :src — a literal src="/assets/..." in a
// template makes Vite try to resolve and bundle it at build time, which fails.
export const LOGO_URL = '/assets/bb_tution_management/images/logo-192.png'
